id: id
name: branch_protection
description: Default branch protection policy

resource: repository
where: test

configuration:
  branchProtectionRules:
    - branchNamePattern: "main"
      requiredApprovingReviewsCount: 4
      dismissStaleReviews: true
